# Evolving Agent PoC

This is a proof-of-concept project showing how a LangGraph agent can evolve tools and prompts when it fails tasks.

## Setup
```bash
pip install -r requirements.txt
ollama pull mistral
```

## Run
```bash
python main.py
```